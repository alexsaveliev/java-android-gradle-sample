This directory contains an extract from Android SDK. 
We need it to make tests pass on all the supported platforms, 
otherwise expected and actual data may be different because of 
different locations of Android SDK on the target platform.